# =============================================================================
# USS Claude — Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Server-side (docker-compose on the homelab)
# -----------------------------------------------------------------------------

# Port the bridge server listens on (inside the container it's always 3420,
# this controls the host-side mapping)
BRIDGE_PORT=3420

# Shared secret — must match MCP_BRIDGE_TOKEN on the laptop
BRIDGE_TOKEN=change-me-to-a-secure-token

# Optional UI password (leave empty to disable)
BRIDGE_PASSWORD=

# -----------------------------------------------------------------------------
# Laptop-side (hook CLI on the machine running Claude Code)
# -----------------------------------------------------------------------------
# These are NOT used by docker-compose — they document what to export on your
# laptop so the hook CLI can reach the homelab bridge server.
#
#   export MCP_BRIDGE_SERVER="ws://<homelab-ip>:3420"
#   export MCP_BRIDGE_TOKEN="<same token as BRIDGE_TOKEN above>"
